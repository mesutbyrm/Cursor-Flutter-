# FAZ 13 — Release build

**Durum:** AUTOMATED_PASS — CI `build-apk.yml` + `apk-latest` aktif

| Alan | Durum |
|------|--------|
| APK CI | ✅ `build-apk.yml` |
| apk-latest release | ✅ |
| Acceptance gate | ✅ 20 madde |
| Signing secrets | repo admin |

**Sürüm:** `mobile/pubspec.yaml` — CI otomatik bump
