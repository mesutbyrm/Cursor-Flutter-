#!/bin/bash
cd /workspace
git add mobile/lib/features/voice_hub/presentation/widgets/premium_2026/
git commit -m "fix(mobile): discover hub VIP import paths and PK mic seat typo"
git push origin main
